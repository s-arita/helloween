body {
  background-image: url(<%= asset_path "halloweenbackground.jpg" %>);
background-repeat: no-repeat;
background-attachment: fixed;
background-position: left bottom;
background-size:auto;
}

.navbar-brand {
    background-image: url(<%= asset_path "logo.gif" %>);
  no-repeat left center;
  background-size: contain;
  height: 50px;
  width: 227px;
}

div .thumbnail {
  opacity: 0.95;
}